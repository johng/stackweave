/* compat.h -- the standard fixed-width / bool / size_t includes, plus plat.h,
 * that the low-level headers (coro, fcontext, cldeque, netpoll) share.
 */
#ifndef RUNLOOM_COMPAT_H
#define RUNLOOM_COMPAT_H

#include "plat.h"

#include <stddef.h> /* size_t, NULL */
#include <stdint.h>
#include <stdbool.h>

#endif /* RUNLOOM_COMPAT_H */
